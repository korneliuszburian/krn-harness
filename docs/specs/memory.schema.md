# Memory Schema

## Purpose

Memory is governed local recall, not automatic truth.

## Fields

- `id`: memory id.
- `summary`: concise memory content.
- `status`: `pending`, `approved`, or `rejected`.
- `evidencePath`: optional source evidence.

## Rule

P0 does not auto-approve memory.
