# WordPress ACF Baseline Probe

Do not use KRN-specific instructions, skills, hooks, or CLI commands for this run.

Task:
Update the synthetic WordPress/ACF theme fixture according to the selected `fixtures/dogfood/tasks/wp-*.json` task. In the final response, list touched files, verification performed, forbidden files avoided, and any missing artifact evidence.

This prompt is comparison-only. It should not count as KRN compliance unless local artifacts prove KRN was used.
