# Current Pricing Behavior

Invoice totals are formatted from integer cents into two decimal places.

The source implementation in `src/index.ts` is the behavior owner. The paired
`src/index.test.ts` file is the local verification oracle.
