# Visual Requirements

The current hero must use the `hero--classic` variant.

Required dimensions:

- mobile hero media minimum height: 320px
- desktop hero media minimum height: 520px
- card grid gap: 24px

The hero call-to-action selector is `[data-hero-cta]`.
