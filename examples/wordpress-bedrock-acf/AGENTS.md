# AGENTS.md

This repository uses KRN Harness.

- Run `krn status` before non-trivial work.
- Run `krn start "<task>"` and `krn context` before edits.
- Respect STOP.
- Run `krn verify` and `krn handoff` after edits.
